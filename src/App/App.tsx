import './App.scss';
import { Button } from '../components';

function App() {
  return (
    <div className="App">
      <Button type={'button'} value={'버튼'} style={null}/>
    </div>
  );
}

export default App;
